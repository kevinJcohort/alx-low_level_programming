#include "main.h"

/**
 * @brief _abs - computes absolute valve of an integer
 * Return: 0
 * 
 * @x: integer
 */
int _abs(int x)
{

    if (x < 0)
    {
        return (x* -1);
    }
    return (x);
}
