#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * _isdigit - checks for a digit
 * return: int
 * @c
 *
 */

int _isdigit(int c)
{
	if (_isdigit(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

