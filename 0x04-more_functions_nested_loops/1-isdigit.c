#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * _isdigit - return 1 if c is a digit or 0 if c is not 
 * return: int
 * @c: input
 *
 */

int _isdigit(int c)
{
	if (_isdigit(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
