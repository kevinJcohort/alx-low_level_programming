#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * _isdigit - return 1 is c is a digit or 0 if not
 * @c: input
 * Return: int
 */

int _isdigit(int c)
{
	if (_isdigit(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
