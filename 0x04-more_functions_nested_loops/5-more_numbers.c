#include "main.h"

#include "main.h"
/**
 * more_numbers - prints 10 times the numbers 0 to 14
 *
 * Return: 0
 */
int i, x

void more_numbers(void)
{
	if (i = 0; i < 10; i++)
	{
		for(x = 0;x < 15; x++)
		{
			_putchar('0', x++);
		}
		_putchar('0', i++);
	}

	_putchar('\n');
}
