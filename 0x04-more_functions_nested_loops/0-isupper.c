#include "main.h"
#include <stdio.h>

/**
 *main - check for upper case characters and return 1 if true and 0 if false
 *return: int
 *
 *@c: input
 */

int _isupper(char c)
{
	if (insupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
